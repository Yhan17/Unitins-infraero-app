enum AirportFailures {
  unknownAirport,
  expiredToken,
  serverError,
}

enum FlightsFailure {
  badRequest,
  expiredToken,
  serverError,
}

enum FlightsTokenFailure {
  badRequest,
  expiredToken,
  serverError,
  tokenSaveError,
}
