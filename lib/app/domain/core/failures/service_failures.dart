enum AirportFailures {
  unknownAirport,
  badRequest,
  expiredToken,
  serverError,
}

enum FlightsFailure {
  unknownFlights,
  badRequest,
  expiredToken,
  serverError,
}

enum FlightsTokenFailure {
  clientIdOrKeyNotFound,
  badRequest,
  expiredToken,
  serverError,
}
